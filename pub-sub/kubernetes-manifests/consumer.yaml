apiVersion: v1
kind: Pod
metadata:
  name: zbio-sample-consumer
  labels:
    app: zbio-sample-consumer
spec:
  containers:
  - name: consumer
    image: zbio-example/consumer:v1
    env:
    - name: SERVICE_ADDRESS
      value: "zbio-service.zbio:50002"
    resources:
      requests:
        cpu: 200m
        memory: 180Mi
      limits:
        cpu: 300m
        memory: 300Mi
    imagePullPolicy: Never
  restartPolicy: OnFailure
  terminationGracePeriodSeconds: 0